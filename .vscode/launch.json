{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${fileDirname}/__debug_bin",
            "preLaunchTask": "go: build (debug)",
            "postDebugTask": "go: remove debug binary"
        }
    ],
}
